{# app/Resources/views/emails/commitmentConfirmation.txt.twig #}
{% if ViewModel.User.Gender == 'M' %}
    Lieber {{ ViewModel.User.Forename }}
{% else %}
    Liebe {{ ViewModel.User.Forename }}
{% endif %}

Du hast dich für den Event {{ ViewModel.Event.Name }} eingetragen.

VIELEN DANK!

Der Event findet statt am {{ ViewModel.Event.Date|date('d.M.Y') }}.

Diese Anmeldung ist verbindlich. Wir erwarten, dass du dir für die Dauer des Events
Zeit reservierst. Wir werden dich rechtzeitig über das weitere Vorgehen informieren.

{% if ViewModel.Department %}
    Du hast als gewünschtes Einsatzressort {{ ViewModel.Department.Name }} angegeben.
    Bitte beachte, dass wir dir keinen Einsatz in deinem gewünschten Ressort garantieren können.
{% endif %}

Du kannst mit folgender URL jederzeit Details zum Event abrufen:
{{ absolute_url(path('event_show', { 'id': ViewModel.Event.Id })) }}

Liebe Grüsse:
das Clanx Team
