{# app/Resources/views/emails/commitmentConfirmation.html.twig #}
{# ViewModel is of type "CommitmentConfirmationViewModel" #}
<h3>
{% if ViewModel.User.Gender == 'M' %}
    Lieber
{% else %}
    Liebe
{% endif %}
{{ ViewModel.User.Forename }}
</h3>

{% set event_url = absolute_url(path('event_show', { 'id': ViewModel.Event.Id })) %}

<p> Du hast dich für den Event
    <a href="{{ event_url }}"><strong>{{ ViewModel.Event.Name }}</strong></a>
    eingetragen.</p>

<p><span style="font-size: 150%">VIELEN DANK!</span></p>

<p> Der Event findet statt am <strong>{{ ViewModel.Event.Date|date('d.M.Y') }}</strong>.</p>
<p> Diese Anmeldung ist verbindlich. Wir erwarten, dass du dir für die Dauer des Events
Zeit reservierst. Wir werden dich rechtzeitig über das weitere Vorgehen informieren.</p>
{% if ViewModel.Department %}
    <p> Du hast als gewünschtes Einsatzressort <strong>{{ ViewModel.Department.Name }}</strong> angegeben.</br>
        Bitte beachte, dass wir den Einsatz in deinem gewünschten Ressort nicht garantieren können.</p>
{% endif %}

<p>Liebe Grüsse: </p>
<h3>das Clanx Team</h3>
