{% extends 'base.html.twig' %}

{% block title %}{{title}}{% endblock %}

{% block body %}

<table class="table table-striped">
    <thead>
        <tr>
            {% for col in columns %}
                <th>
                    {{col|nl2br}}
                </th>
            {% endfor %}
        </tr>
    </thead>
    <tbody>
        {% for row in rows %}
            <tr>
                {% for cell in row %}
                    <td>
                        {{cell|nl2br}}
                    </td>
                {% endfor %}
            </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}
