
# PLACEHOLDERS : USE IT LIKE this
# label.hello_name: Hello %name%
# IN CODE:
# $translated = $translator->trans('Hello %name%', array('%name%' => $name));


'btn.save': Save
'btn.cancel': Cancel

'tip.save_and_return': Saves "%entity%" and returns to "%return%"
'tip.cancel_and_return': Discards changes on "%entity%" and returns to "%return%"
