{% extends 'base_nav.html.twig' %}

{% block title %}
Passwort ändern
{% endblock title %}

{% block workspace %}
    {{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
        {{ form_widget(form) }}
        <div class="form-group">
            <button type="submit" class="btn btn-default">Passwort setzen</button>
        </div>
    {{ form_end(form) }}
{% endblock workspace %}
