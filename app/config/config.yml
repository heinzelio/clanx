imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

# Put parameters here that don't need to change on each machine where the app is deployed
# http://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration

framework:
    templating:
        engines: ['twig']
    trusted_hosts:   ~
    fragments:       ~
    http_method_override: true
    assets: ~

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
    form_themes:
        # Forms are automatically constructed.
        # With this switch, we can use BOOTSTRAP (Yay!)
        # (we can even go further! see https://symfony.com/doc/current/book/forms.html#id1)
        - bootstrap_3_layout.html.twig
        #- bootstrap_3_horizontal_layout.html.twig
    globals:
        flash: "@AppBundle\\Service\\Flash\\FlashService"

# Doctrine Configuration
doctrine:
    dbal:
        driver:   pdo_mysql
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: "%kernel.root_dir%/data/data.db3"
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #     path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: "%mailer_transport%"
    host:      "%mailer_host%"
    username:  "%mailer_user%"
    password:  "%mailer_password%"
    spool:     { type: memory }
    encryption: ssl
# app/config/config.yml

fos_user:
    db_driver: orm
    firewall_name: main
    user_class: AppBundle\Entity\User

    from_email:
        # ALL different emails use this adress and name:
        address:        no-reply@clanx.ch
        sender_name:    ClanxHölferDB
    registration:
        confirmation:
            enabled:    true
    profile:
      form:
        type: AppBundle\Form\ProfileFormType
